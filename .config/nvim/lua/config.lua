-- global config to enable or disable plugins and/or features
--

return {
	dap_virtual_text = true, -- dap-ui can add virtual text to indicate variable value change etc
    null_ls_debug = false,   -- null-ls log level
}
